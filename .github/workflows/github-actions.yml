name: CI for Java Invoice
on: [push]
jobs:


  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
      - name: Test
        run: mvn test

checkstyle:
          name: Check with checkstyle
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v2
            - name: Set up JDK
              uses: actions/setup-java@v2
              with:
                java-version: '17'
                distribution: 'adopt'
            - name: Checkstyle
              run: mvn checkstyle:check
>>>>>>> 4af990e7daabe99cf26f5c4f82ef80dfce1f1a55
